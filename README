##todo deploy.sh
Flask, Parameter Store, CloudWatch, S3, Jenkins, Makefile, Docker, API, RDS

1. Update file .env file with your variables:
    FLASK_ENV=choose one: 'dev', 'docker' or 'prod'
    LOCAL_DB_NAME='my_super_db'
    PARAMETER_STORE = mysql+pymysql://<db_user_name>:<password>@<endpoint>:<port>/<db_name>
or
1. as above
4. docker-compose down
5. docker-compose up -d
